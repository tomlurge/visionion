!!!!	when aggregating fake data, don't forget to adjust collection names in script
	around lines 361 and 369


TODO

	dailyFakes span ist "h" statt "d"

	_id des weekly fake ist "undefined" 
		weil nur daily oder monthly keys definiert werden (zeile 90ff)


	client	wenigstens durch 24 dividieren
		fakesMR zeile 171 
		if client && daily then just replace value by "new" value, don't +=

	daily und monthly für alle jahre faken

	weekly fake enthält AS 466704 zweimal, mit leicht unterschiedlichen daten
		(dieses AS gibt es ausserdem nicht und es ist in importData nicht enthalten)

	mongodb stürzt bei größeren jobs ab
		daily aggregation zb läuft für einen tag oder einen monat durch
		aber für einen monat gab es schon fehlermeldungen wg. query too large
			Sat Jul 18 23:48:31.886 [conn17] warning: ClientCursor::yield can't unlock b/c of recursive lock ns: visionion.tmp.mr.fakes_13 top: { opid: 820, active: true, secs_running: 2496, op: "query", ns: "visionion.dailyFakes", query: { $msg: "query not recording (too large)" }, client: "127.0.0.1:54294", desc: "conn17", threadId: "0x104ca9000", connectionId: 17, locks: { ^: "W" }, waitingForLock: false, msg: "m/r: merge post processing M/R Merge Post Processing Progress: 1/31 3%", progress: { done: 1, total: 31 }, numYields: 11, lockStats: { timeLockedMicros: { r: 10639630, w: 1139373 }, timeAcquiringMicros: { R: 0, W: 1, r: 1199343322, w: 300 } } }
		in der art 7 fehler, bei tag: 1, 3, 7, 9, 14, 16, 30
	für ein jahr stürzt die datenbank tatsächlich ab und erzeugt keine ergebnisse


	ich brauche also skripte, die die aggregierung steuern
	die dann auch als zusatznutzen zb die unterteilung _id/value lösen könnten

	eventuell muss monthly auf daily statt hourly aufsetzen
		auch monthly auf basis von hourly stürzt ab (nach 10 stunden)
		das erfordert allerdings eigentlich auch, dass ein monthly job zuerst 
			prüft, ob alle dailys aktuell sind
		aber erstmal checken, wie lamge monthly läuft, wenn es AS richtig aggregiert

daily dauert 42 minuten für januar 2007 und wirft obige fehler